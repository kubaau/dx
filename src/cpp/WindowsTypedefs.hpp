#pragma once

#include <Windows.h>

using Point = POINT;
using ButtonState = WPARAM;
using Width = UINT;
using Height = UINT;
using Title = LPCSTR;
using WindowHandle = HWND;
using InstanceHandle = HINSTANCE;
